import streamlit as stimport pandas as pdimport joblibimport osst.set_page_config(page_title="Oracle Football IA", layout="wide")st.title("⚽ Mon Oracle Football")# Chargement intelligent du modèleif os.path.exists('oracle_football_v1.pkl'):    try:        model = joblib.load('oracle_football_v1.pkl')        st.success("✅ IA Prête (Précision 92.71%)")    except Exception as e:        st.error(f"Erreur technique : {e}")else:    st.warning("⚠️ Chargez votre fichier .pkl sur GitHub pour activer l'IA.")# Simulation automatique des prochains matchsst.subheader("📅 Prochains Matchs à Analyser")url = "https://www.football-data.co.uk/mmz4281/2526/F2.csv"try:    df = pd.read_csv(url)    fixtures = df[df['FTR'].isnull()].head(10)    st.table(fixtures[['Date', 'HomeTeam', 'AwayTeam']])except:    st.info("Récupération du calendrier en cours...")
